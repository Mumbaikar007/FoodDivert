<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhishek.fooddivert">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register_page"
            android:theme="@style/Trans" />
        <activity
            android:name=".AlredyRegister"
            android:theme="@style/Trans" />
        <activity android:name=".Donor_or_Receiver" />
        <activity android:name=".Donor_user_info" />
        <activity android:name=".Donor_user_info2" />
        <activity android:name=".Receiver_retrive_information" />
        <activity android:name=".Receiver_click_activity" />
        <activity android:name=".Call_or_msg" />
        <activity android:name=".Doners_Current_Status" />
        <activity
            android:name=".About"
            android:theme="@style/FullB" />
        <activity android:name=".Transport_hiring" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme" />
        <activity android:name=".Food_image" />
        <activity android:name=".Receiver_food_image" />
        <activity android:name=".Doners_Listview" />
        <activity android:name=".NGO_contacts" />
        <activity android:name=".Receiver_status"></activity>
    </application>

</manifest>